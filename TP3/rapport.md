# Rapport — TP3

## Points clés

### 1. Données

Permer d'afficher une liste de projetssous forme de cartes HTML grâce à :
const carte = document.createElement('article');
    carte.classList.add('carte');

### 2. Filtrage

Filtrer les projets selon le tag cliquéen utilisant :
 const tag = btn.dataset.tag;

### 3. Ajout et sauvegarde

Ajouter le fromulaire dans un premier temps avec : 
  projets.push(nouveauProjet);
Puis pour le sauvegarder au chargement de la page il suffit de rajouter la commande :  sauvegarder ()
